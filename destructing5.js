let array1=[10,20,80,5];
function minMax()
{
    const maxi=Math.max(...array1);
    console.log("maximum is"+maxi);
    const min=Math.min(...array1);
    console.log("maximum is"+min);
}
minMax();
